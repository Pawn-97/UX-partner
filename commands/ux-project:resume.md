# /ux-project:resume

Resume an existing UX discovery project.

## Arguments

- `project_name`: required project folder name under `projects/`.

## Workflow

1. Read `skills/ux-discovery/SKILL.md`.
2. Read `.claude-plugin/commands/resume.md`.
3. Execute the canonical command with the user arguments, applying the Codex adapter rules.
4. Keep resume read-only until the user chooses a next action.
